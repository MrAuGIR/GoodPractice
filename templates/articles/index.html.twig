{% extends "layout.html.twig" %}
    {% block content  %}
        <main>
            <div class="row sec-background-1">
                <div class="container">
                    <div class="row mt-5">
                        <div class="col-12">
                            <h1>Derniers Articles ajoutés</h1>
                        </div>
                    </div>
                    <div class="row mt-2 mb-5 pt-2 pb-1">
                        {% set index = 0 %}
                        {% for card in cards %}
                            {% if index == 0 %}
                                {{card.generateMainCard() | raw}}
                                {% set index = 1 %}
                            {% else %}
                                {{ card.generateCard() | raw}}
                            {% endif %}
                            
                        {% endfor %}
                    </div>
                </div>  
            </div>
            <div class="row sec-background-2">
                <div class="container">
                    <div class="row my-5">
                        <div class="col-12">
                            <span class="subtitle">Ajouter un article ?</span>
                            <a href="?controller=article&action=add" class="btn btn-primary mx-2">Ajouter</a>
                        </div>
                    </div>
                </div>
            </div>
            <div class="row sec-background-1">
                <div class="container">
                    <div class="row my-5">
                        <div class="col-12">
                            <h1>Catégories</h1>
                        </div>
                        <div class="col-12">
                            <div class="row">
                                {% for categorie in category %}
                                    <a href="?controller=article&action=list&category={{categorie.getId_category()}}" class="badge badge-primary px-2 py-2 mx-2">{{categorie.getName_Category}}</a>
                                {% endfor %}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            {% set number = "2" %}
            {% for key, category in listArticleBestCategory  %}
            <div class="{{'row sec-background-'~ number}}">
                <div class="container">
                    <div class="row my-5">
                        <div class="col-12">
                            <h2>{{ key }}</h2>
                        </div>
                        <div class="col-12">
                            <div class="row">
                                {% for card in category %}
                                    {{ card.generateCard() | raw}}
                                {% endfor %}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            {% set number = (number == "2")? "1":"2" %}
            {% endfor %}
            
        </main>
    {% endblock %}