# Build de prod (hébergement IONOS « à plat », sans mod_rewrite).
# L'API est servie via le front-controller Symfony en PATH_INFO.
# Si le sous-domaine supporte mod_rewrite, repasser à /api et rebuild.
VITE_API_BASE=/index.php/api
