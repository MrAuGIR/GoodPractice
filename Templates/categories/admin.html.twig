    {% extends "layout.html.twig" %}
    {% block content  %}
    <main>
        <div class="container">
            <div class="row">
                <h1>Tableau de Bord</h1>
            </div>
            <div class="row">
                <div class="col-12 col-md-2 border-end">
                    <div class="d-flex align-items-start">
                        <div class="nav flex-column nav-pills me-3" id="v-pills-tab" role="tablist" aria-orientation="vertical">
                            <a class="nav-link" href="?controller=admin&action=adminArticle" >Mes articles</a>
                            <a class="nav-link" href="?controller=admin&action=adminCategory">Les categories</a>
                        </div>
                    </div>
                </div>
                <div class="col-12 col-md-10">
                    <table class="table dashboard-table">
                        <thead>
                            <tr>
                                <th scope="col">Id</th>
                                <th scope="col">Nom</th>
                               <td>Actions</td>
                            </tr>
                        </thead>
                        <tbody>
                            {% for category in categories %}
                            <tr>
                                <td>{{category.getId_category}}</td>
                                <td>{{category.getName_Category}}</td>
                                <td>
                                    <a href="?controller=category&action=edit&q={{category.getId_category}}">Edit</a><br>
                                    <a href="?controller=category&action=delete&q={{article.getId_category}}">Delete</a>
                                </td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </main>
    {% endblock %}