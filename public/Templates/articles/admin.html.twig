{% extends "layout.html.twig" %}
    {% block content  %}
    <main>
        <div class="container">
            <div class="row">
                <h1>Tableau de Bord</h1>
            </div>
            <div class="row">
                <div class="col-12 col-md-2 border-end">
                    <div class="d-flex align-items-start">
                        <div class="nav flex-column nav-pills me-3" id="v-pills-tab" role="tablist" aria-orientation="vertical">
                            <a class="nav-link" href="?controller=admin&action=adminArticle" >Mes articles</a>
                            <a class="nav-link" href="?controller=admin&action=adminCategory">Les categories</a>
                        </div>
                    </div>
                </div>
                <div class="col-12 col-md-10">
                    <table class="table dashboard-table">
                        <thead>
                            <tr>
                                <th scope="col">Titre</th>
                                <th scope="col">Description</th>
                                <th scope="col">Date creation</th>
                                <th scope="col">Catégorie</th>
                                {% if user.getRole == LEVEL_ADMIN %} 
                                    <th scope="col">utilisateur</th>
                               {% endif %}
                               <td>Actions</td>
                            </tr>
                        </thead>
                        <tbody>
                            {% for article in articles %}
                            <tr>
                                <td>{{article.getTitle}}</td>
                                <td>{{ article.getDescription | slice(0,50) | raw }}</td>
                                <td>{{ article.getDate_create }}</td>
                                <td>{{ article.getCategory.getName_Category }}</td>
                                {% if user.getRole == LEVEL_ADMIN %} 
                                    <td>{{ article.getUser.getName() }}</td>
                                {% endif %}
                                <td>
                                    <a href="?controller=article&action=show&q={{article.getId_article}}">show</a><br>
                                    <a href="?controller=article&action=edit&q={{article.getId_article}}">Edit</a><br>
                                    <a href="?controller=article&action=delete&q={{article.getId_article}}">Delete</a>
                                </td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </main>
    {% endblock %}